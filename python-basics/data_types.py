user_age=21 #This is integer variable that stores the age of the user

user_name="Gaurav" #This is string variable that stores the name of the user

message=f"Hello I am {user_name} and my age is {user_age}."#This is a variable that stores a message using f-string formatting

print(message)

print(type(message)) #This will print the type of the variable message, which is a string (str)

print("Hello I am",user_name,"and my age is",user_age)#This will print the message using comma-separated values, which will automatically add spaces between them.